# =============================================================================
# ACTION ITEM GRAPH PIPELINE - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your credentials

# -----------------------------------------------------------------------------
# OpenAI Configuration
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-...

# Model selection (optional - defaults shown)
# OPENAI_CHAT_MODEL=gpt-4.1-mini          # For extraction and synthesis
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # For action item embeddings
# OPENAI_EMBEDDING_DIMENSIONS=1536        # Embedding vector dimensions

# -----------------------------------------------------------------------------
# AI Database — Neo4j (Action Items, Topics, Owners)
# This is a SEPARATE Neo4j instance from the Deal DB below.
# -----------------------------------------------------------------------------
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io  # Aura format
# NEO4J_URI=bolt://localhost:7687                 # Local format
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password
NEO4J_DATABASE=neo4j                              # Optional, defaults to "neo4j"

# -----------------------------------------------------------------------------
# AI Pipeline Configuration (optional)
# -----------------------------------------------------------------------------
# SIMILARITY_THRESHOLD=0.65               # Threshold for action item matching (0.0 - 1.0)
# LOG_LEVEL=INFO                          # DEBUG, INFO, WARNING, ERROR
# LOG_FORMAT=json                         # json or console

# -----------------------------------------------------------------------------
# Deal Database — Neo4j (Deals, DealVersions, Interactions, Accounts)
# This is a SEPARATE Neo4j instance from the AI DB above.
# Each pipeline has its own client, schema, and constraints.
# -----------------------------------------------------------------------------
DEAL_NEO4J_URI=neo4j+s://yyyyyyyy.databases.neo4j.io
DEAL_NEO4J_USERNAME=neo4j
DEAL_NEO4J_PASSWORD=your-deal-password
DEAL_NEO4J_DATABASE=neo4j

# Deal Pipeline Thresholds (optional)
# DEAL_SIMILARITY_THRESHOLD=0.70          # Vector match threshold (0.0 - 1.0)
# DEAL_AUTO_MATCH_THRESHOLD=0.90          # Auto-match (skip LLM) threshold